# =================================================
# PostgreSQL Connection Configuration for AI Agent
# =================================================
# This file is sourced by the check scripts to get database credentials.
# For production environments, consider using a more secure method
# like a .pgpass file or IAM roles (for cloud databases).

# The database server hostname or IP address.
export PGHOST="127.0.0.1"

# The port the database server is listening on.
export PGPORT="1922"

# The PostgreSQL user to connect as.
export PGUSER="digoal"

# The password for the user.
# WARNING: Storing passwords in plaintext is a security risk.
# This is for demonstration purposes. Use .pgpass in production.
export PGPASSWORD="123456"

# The specific database to connect to for the checks.
export PGDATABASE="postgres"
